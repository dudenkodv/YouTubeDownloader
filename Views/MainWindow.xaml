<Window x:Class="YouTubeDownloader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:YouTubeDownloader.Views"
        Title="YouTube Downloader" Height="650" Width="750"
        WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ссылка YouTube:" FontWeight="Bold" Margin="10,10,0,5"/>
        <TextBox x:Name="UrlTextBox" Text="{Binding Url, UpdateSourceTrigger=PropertyChanged}" 
                 Grid.Row="1" Margin="10,0,10,10" Height="30"/>

        <WrapPanel Grid.Row="2" Margin="10,0,0,10">
            <Button Content="Загрузить информацию" Width="150" Height="30" 
                    Command="{Binding LoadCommand}" Margin="0,0,10,0"/>
            <Button Content="Отмена" Width="80" Height="30" 
                    Command="{Binding CancelCommand}" Margin="0,0,10,0"/>
            <Button Content="Логи" Width="80" Height="30" 
                    Command="{Binding OpenLogsCommand}" Margin="0,0,10,0"/>
            <Button Content="Открыть папку" Width="110" Height="30" 
                    Command="{Binding OpenFolderCommand}" Margin="0,0,10,0"/>
            <Button Content="Проверить обновления" Width="150" Height="30" 
                    Command="{Binding CheckUpdatesCommand}"/>
        </WrapPanel>

        <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="10,0,0,10">
            <RadioButton Content="Видео" IsChecked="{Binding IsVideoMode}" Margin="0,0,15,0"/>
            <RadioButton Content="Аудио (MP3)" IsChecked="{Binding IsAudioMode}"/>
        </StackPanel>

        <ListBox ItemsSource="{Binding Formats}" SelectedItem="{Binding SelectedFormat}"
                 DisplayMemberPath="DisplayName" Grid.Row="4" Height="150" Margin="10,0,10,10"/>

        <!-- Активные задачи -->
        <GroupBox Header="Активные задачи" Grid.Row="5" Margin="10,0,10,10">
            <ScrollViewer MaxHeight="200">
                <ItemsControl ItemsSource="{Binding ActiveTasks}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <local:ProgressTaskControl Task="{Binding}" 
                                CancelCommand="{Binding DataContext.CancelTaskCommand, RelativeSource={RelativeSource AncestorType=Window}}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>

        <Grid Grid.Row="6" Background="#80FFFFFF">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsSpinnerVisible}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar Width="500" Height="50" IsIndeterminate="True" />
                <TextBlock Text="{Binding SpinnerText}" Margin="0,10,0,0" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <ProgressBar Value="{Binding ProgressValue}" Height="5" Margin="10,0,10,10" Grid.Row="7">
            <ProgressBar.Style>
                <Style TargetType="ProgressBar">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding IsProgressVisible}" Value="True">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ProgressBar.Style>
        </ProgressBar>

        <StackPanel Grid.Row="8" Orientation="Horizontal" Margin="10,10,10,10">
            <TextBlock Text="{Binding StatusText}" VerticalAlignment="Center" Width="350" Margin="0,0,15,0"/>
            <Button Content="Добавить в очередь" Width="130" Height="30" 
                    Command="{Binding AddToQueueCommand}" Margin="0,0,10,0"/>
            <Button Content="Скачать" Width="100" Height="30" 
                    Command="{Binding DownloadCommand}" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>